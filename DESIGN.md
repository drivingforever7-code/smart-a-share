# 智选 A 股 — 视觉身份

## Style Prompt

采用 Data Drift 风格的量化数据驾驶舱：深空蓝黑画布承载冷静、可信的数据界面，青色作为主要交互与数据高光，紫色只用于环境光和层次；卡片像半透明仪表面板，边界清晰但不过度发光。动画应连续、轻量、有方向感，帮助用户理解页面切换和交互反馈，不遮挡信息，也不改变任何功能。

## Colors

- 画布：`#050914`
- 面板：`#091323`
- 主文字：`#E8F3FF`
- 主强调：`#35D9FF`
- 环境强调：`#8B5CF6`
- A 股上涨：`#F04438`
- A 股下跌：`#12B76A`

## Typography

- 中文与正文：`Microsoft YaHei UI`, `PingFang SC`, system-ui, sans-serif
- 数字、代码与指标：`Consolas`, `SFMono-Regular`, monospace
- 标题使用 700–800 字重，正文使用 350–400 字重；深色背景增加行高，数字启用等宽特性。

## Motion

- 页面进入：`0.42–0.72s`，按“应用外壳 → 标题与工具栏 → 主要卡片 → 次级数据”分阶段出现，使用统一减速曲线。
- 页面切换：用轻微纵向位移、透明度和一次性扫描光完成衔接；不改变布局尺寸，避免页面跳动。
- 数据加载：显示数据核心、轨道、扫描扇区与脉冲轨迹，明确说明正在同步或校验的数据，不使用虚假百分比。
- 图表加载：保留原图表空间，避免加载完成时页面位移；加载动画不得遮挡错误、空数据和缓存状态。
- 交互反馈：`0.18–0.28s`，按钮、菜单、表格行和卡片只移动 1–3px。
- 环境动画：`14–24s` 的低透明度光晕与网格漂移，不影响阅读。
- 遵守 `prefers-reduced-motion`，用户要求减少动画时关闭非必要动效。

## What NOT to Do

- 不使用高频闪烁、故障抖动或大面积霓虹。
- 不给所有文字使用发光或渐变。
- 不改变页面结构、功能、字段、数据颜色含义和操作路径。
- 不让装饰层拦截鼠标或覆盖内容。
- 不以动画替代加载、错误和风险提示。
