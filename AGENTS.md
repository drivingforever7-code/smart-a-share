# 智选 A 股 — 产品与开发文档

> 本文档是项目的唯一权威文档。产品需求、技术决策、数据规则和协作规则均以本文档为准。
> 任何需求、数据库结构或重要设计发生变化时，必须先更新本文档，再修改代码。

---

## 一、产品概述

| 项目 | 说明 |
|------|------|
| 暂定名称 | 智选 A 股 |
| 产品形态 | 本地运行的量化选股网站 |
| 未来方向 | 代码托管至 GitHub，并升级为可公开访问的网站 |
| 目标市场 | 沪深主板、创业板、科创板、北交所 |
| 核心用途 | 短线与波段选股、综合评分、买入建议、风险解释、历史验证 |
| 目标用户 | 会看少量股票数据，但不懂编程的普通投资者 |
| 运行方式 | Windows 本机启动服务后，通过浏览器访问 |
| 数据原则 | 第一版以 AKShare 免费数据为主；所有行情必须显示数据时间 |

网站帮助用户回答五个问题：

1. 今天有哪些股票值得关注？
2. 哪些股票符合我的短线或波段条件？
3. 系统为什么给这只股票这样的分数？
4. 系统为什么建议买入、观察或暂不参与？
5. 相同条件在历史上的表现和风险如何？

核心路径：

    更新市场数据 → 选择短线或波段 → 查看机会或设置筛选条件
    → 查看评分、建议与风险 → 查看股票详情 → 加入自选或验证历史表现

### 1.1 产品原则

1. 解释优先：任何评分和建议必须展示依据，不能只给结论。
2. 风险同屏：买入理由、风险、参考价位和失效条件放在一起。
3. 时间透明：实时、分钟、日线和财务数据分别显示最后更新时间。
4. 真实数据优先：获取失败时明确报错或显示缓存时间，禁止模拟数据冒充行情。
5. 新手可读：专业指标提供简短中文解释。
6. 本地优先、平滑升级：接口和数据层要为未来公共网站预留迁移能力。

---

## 二、V1 功能范围

### 2.1 今日机会

- 短线机会榜与波段机会榜
- 从今日机会进入股票详情后返回时，必须恢复进入前的短线/波段模式、策略筛选和页面滚动位置，不得重新跳回短线榜顶部
- 短线与波段分别最多展示 10 只；只有通过硬风险与最低置信度门槛的股票才能进入，数量不足时允许少于 10 只，禁止为了凑数降低标准
- 交易时间每 10 秒刷新最新价格、涨跌幅和行情时间
- 展示总分、分项分、建议等级、核心理由和风险标签
- 按市场、行业、评分和涨跌幅过滤
- 加入或移出自选股
- 默认排除 ST、退市整理、停牌和上市不足 120 天的股票，设置中可打开

### 2.2 板块热力图

- 新增独立板块热力图页面，条件选股页面与独立股票详情导航取消
- 以实时排名、热力矩形、资金流向图和涨跌分布展示行业及概念板块
- 每个板块展示涨跌幅、上涨/下跌家数、平均涨跌、强度、热度、成交额、主力净流入及数据时间；交易时间默认每 10 秒检查更新
- 板块建议由可解释量化评分与可选 DeepSeek 复核共同生成，只输出买入关注、观望和卖出/回避，并同时列出依据、风险与失效条件
- 板块可点击下钻，展示简介、核心股票、资金龙头、情绪标的和完整成分排名；其中股票均可继续打开现有股票详情
- 板块数据失败时保留最后有效缓存并标注来源与缓存时间，不得用随机值补齐
### 2.3 综合评分榜（已取消）

- 综合评分榜与今日机会重复，页面和导航删除；评分与解释继续保留在今日机会、股票详情和自动回测中

### 2.4 股票详情

- 股票详情继续完整保留，但不再作为主导航板块；从今日机会、自动回测、板块热力图及其他股票列表打开时，左侧导航保持在来源页面，浏览器返回后恢复来源页面与位置
- 所有股票搜索入口统一支持 6 位代码、中文名称或名称片段、完整拼音和拼音首字母；完整代码可直接打开
- 最新价、涨跌幅、成交额、换手率、量比、PE、PB、市值
- 图表只保留分时、日 K、周 K、月 K，不再提供多个分钟 K 切换
- 分时图展示当天价格线、均价线和成交量
- 分时数据按交易时间排序并去重；成交额与成交量单位必须先校准，再计算累计均价
- 分时图显示实际交易日期、数据源和缓存状态；打开分时页后每 10 秒静默刷新
- 日、周、月 K 展示 MA5、MA10、MA20、MA60，并可独立开关
- MACD、RSI、KDJ、布林带和成交量
- 个股页提供策略选择、回测周期和“回测并显示”按钮
- 策略至少包含现有八种、短线综合、波段综合和用户自定义策略
- 回测后在日 K 上标记买点和卖点；悬停显示日期、价格、收益和退出原因
- 图表下方展示收益、回撤、胜率和交易次数，并可清除标记
- 短线与波段评分详情
- 建议、理由、风险、参考关注区间、止损参考和信号失效条件
- 财务、估值、行业及行业内排名

### 2.5 策略验证

- 验证筛选条件和预设方案的历史表现
- 累计收益、年化收益、最大回撤、夏普比率和胜率
- 与沪深 300 同期表现比较
- 收益曲线和信号明细
- 设置持有周期、止盈、止损和交易成本
- 禁止使用未来数据，固定提示历史表现不代表未来收益

### 2.6 已购入

- 自选股功能取消，由“已购入”替代
- 已购入状态保存在浏览器本地，不需要账号，不向公共网站其他访问者公开
- 自动回测股票可标记已购入，并填写实际买入价和买入日期；已购入标的在自动回测页顶部独立显示，支持取消
- 展示实时行情、实际持仓收益、短线分、波段分和当前操作建议

### 2.7 数据与设置

- 查看各类数据更新时间
- 手动更新并显示进度、成功数、失败数和原因
- 实时刷新默认 10 秒，可配置
- 配置是否包含 ST、退市整理、停牌和次新股
- 数据源失败时保留最后有效缓存并明确标注

### 2.8 策略工坊

- 提供独立的策略工坊页面，不要求用户编写代码
- 内置策略可以查看和修改参数，并提供恢复默认值
- 用户可以新建、复制、重命名和删除自定义策略
- 条件指标包括均线、MACD、RSI、KDJ、布林带、涨跌幅、成交量、量比、换手率、PE、PB、ROE、营收增长和利润增长
- 条件运算包括大于、小于、区间、上穿和下穿
- 同一策略内可选择“全部满足”或“任意满足”
- 每个策略分别配置买入条件、卖出条件、止损、止盈、最长持有天数和交易成本
- 用户可以组合多个策略，并为每个策略填写权重
- 组合权重总和必须为 100%，提供自动平均分配
- 组合策略按各子策略信号乘以权重得到综合得分，达到可修改的触发分数后买入
- 内置“短线综合”和“波段综合”策略也使用相同的加权结构
- 策略保存在后端 SQLite，本地版不需要账号

### 2.9 历史财务回测

- 低估值、业绩成长和包含基本面条件的组合策略必须使用历史时点财务数据
- 财务数据按报告期保存，并记录可用于回测的日期
- 有真实公告日期时使用公告日期；缺失时使用保守可用日期，禁止提前使用尚未公开的数据
- 历史 PE、PB 可根据当时股价和报告期每股收益、每股净资产计算，并标注为估算值
- 数据无法取得或关键字段缺失时明确提示，不得用当前财务数据回填过去


### 2.10 AI 联合分析

- AI 是可选增强功能；没有配置密钥时，行情、评分、选股和回测必须继续可用
- 当前在线模型默认使用 DeepSeek，后续保留 OpenAI 兼容接口和本地 Ollama 切换能力
- 采用“技术分析、基本面、市场环境、风险控制、多空辩论、总评”多角色流水线
- AI 输入必须来自页面可核对的数据快照，输出必须列出引用指标、主要理由、反方意见、风险和失效条件
- AI 结论只作为独立参考层，不得伪装成可重复的固定量化策略
- 每日收盘后仅对短线前 20 和波段前 20 各批量复核一次并缓存，指标分占 70%、AI 分占 30%；AI 失败时回退纯指标且不得绕过硬风险
- AI 输出包括强烈看多、看多、中性、看空、强烈看空五档，并给出 0–100 置信度
- 保存分析时间、股票代码、输入快照摘要、模型、结论和完整报告，支持历史查看
- AI 请求失败、余额不足、超时或返回格式异常时明确提示，不得生成假报告
- 个股页增加“AI 联合分析”卡片；设置页增加密钥状态、连通性测试、模型和分析深度

### 2.12 自动回测

- 新增独立“自动回测”页面，分别跟踪每日短线机会榜前三和波段机会榜前三
- 默认展示最近 5 个交易日，并支持切换最近 3、5、10 个交易日
- 每条记录展示发现日期、当日排名、代码、名称、行业、发现价、当前价、截至当前涨跌幅和跟踪自然日
- 主列表只突出发现日期、股票、发现至今涨跌幅和当前操作；评分、依据、风险、行情时间和历史建议放入展开详情
- 自动回测页顶部与炸板研究保持一致的信息层级：关键表现指标、策略版本、样本积累与升级进度必须保留，不能因主表紧凑化而删除
- 短线与波段表现记录必须分别显示；波段在达到 15 个后续有效观察日前仍展示实时跟踪表现、待成熟样本数量和预计条件，不得以“0 个成熟样本”误导为没有回测数据
- 连续 3 个有效交易日均为清仓后从主列表自动隐藏，但保留数据库审计和训练样本
- 分别汇总短线与波段的平均涨跌幅、上涨数量、最佳标的和最弱标的
- 每个交易日北京时间 15:05 后的首次有效综合机会榜作为收盘发现快照；盘中访问不得提前冻结排名，同一模式、同一交易日只保留前三名
- 发现快照必须具有明确的真实行情时间，或由独立交易日历确认的交易日期；不得使用抓取时间、服务器时间或缓存刷新时间直接冒充交易日
- 每次入库前按模式和股票代码去重，并重新连续编号；检测到无行情时间且与上一发现日完全相同的缓存重复榜单时自动清理
- 每条发现记录提供删除按钮；删除仅在当前浏览器隐藏该条记录并保持其他记录原有顺序，不删除审计历史和训练样本
- 公共版由 GitHub Actions 在工作日北京时间 15:20 自动唤醒接口并留存；节假日因行情日期不变不得重复记录
- 每次公共留存成功后，将经交易日期校验的短线前三和波段前三导出为按日期命名的 GitHub 归档；归档只保存发现时快照，不保存密钥或用户数据
- 本地版和公共版在启动及自动回测刷新时自动同步 GitHub 归档，并以日期、模式和排名幂等补入本地数据库；因此本机未开机或 Render 重启后仍能恢复历史发现记录
- GitHub Actions 必须校验最新有效交易日、短线与波段各三条记录以及归档文件实际生成；只有接口返回 200 但没有新增或已归档正确交易日时不得把任务视为成功
- 只使用真实保存的历史发现快照；功能启用前没有记录时明确提示，不得用当前榜单伪造过去发现结果
- 页面刷新时使用最新真实行情更新当前价和累计涨跌幅；行情失败时保留最后有效值并标明缓存状态
- 该板块属于榜单发现表现跟踪，不假设真实成交，不扣滑点、佣金和印花税；完整可成交回测仍以策略实验室为准
- 已发现标的从下一交易日起每天生成一条操作建议，只包括加仓、继续持有和清仓
- 每日操作建议必须展示建议日期、当前评分、发现后涨跌幅、建议仓位、依据、风险和失效条件
- 操作建议按“假设发现日已关注或持有”解释，不得伪装成真实成交记录；同一标的同一交易日只保留一条最终建议

### 2.13 炸板研究

- 新增独立“炸板研究”页面，按交易日展示曾触及涨停后开板的股票，并区分待确认、已回封和未回封
- 当日列表始终按预测时的回封概率/综合评分降序展示；盘中每次刷新必须同时核对炸板池和涨停池，曾炸板且当前重新处于涨停池的股票立即显示“回封成功”，仍处于炸板池的显示“继续监控”，不得等待收盘才更新页面状态
- “回封成功”和“继续监控”属于可随盘中再次开板而变化的实时展示状态；模型准确率、Brier 分数、训练标签和最终复盘仍只使用收盘后冻结的最终结果，禁止用盘中状态污染回测
- 主列表按交易日折叠展示最近有效交易日的合格候选，最新交易日默认展开，其他日期默认折叠；历史有效预测继续进入顶部有效样本、历史实际回封率、方向准确率、Brier 分数、概率分档和模型优化统计
- 每个交易日只保存和统计预测时排名前 10 的合格候选；低于最低概率、数据不完整或风险过高时允许少于 10 只，不得凑数
- 每条记录展示代码、名称、行业、首次封板时间、观测时间、炸板次数、最新价、涨停价、距涨停价、涨跌幅、振幅、换手率、成交额、流通市值、涨停统计和市场封板率
- 盘中对仍处于炸板状态的股票计算回封概率并排名，同时给出建议小仓位试买、仅观察或回避、建议仓位、依据、风险和失效条件
- 回封概率首版使用可解释规则基线，候选因子包括首次封板时间、炸板次数、距涨停价、振幅、换手率、成交额、流通市值、近期涨停次数、连板高度、行业热度和当日市场封板率
- 公共版工作日在午盘和尾盘前保存带时间戳的盘中预测快照，收盘后用涨停股池确认是否回封；盘后才出现的记录可以展示，但不得计入盘中预测准确率
- 当日缺少午盘或尾盘预测快照时，收盘刷新允许补录最多 10 只盘后候选用于页面展示；盘后补录必须标记为不参与盘中预测准确率、Brier 分数和模型训练
- 每日复盘分别列出已回封和未回封股票，说明模型当时看对或看错的主要因子，并汇总回封率、Brier 分数、概率分档命中率和样本数量
- 历史回封率、方向准确度、Brier 分数、样本数量等顶部指标均可点击展开，查看按日期、概率分档、模型版本和个股预测组成的明细；统计口径仅包含每日前 10 个有效盘中预测
- 所有预测必须保存模型版本、特征快照、预测时间和最终结果，禁止用收盘后字段回填盘中预测
- 同一股票同一交易日存在多次盘中预测时，收盘复盘与优化完成后可清理重复的中间快照，但必须保留最终纳入统计的盘中预测、结果、特征、模型版本及模型升级记录，保证历史指标可以复算
- 每日可以基于已完成结果训练候选逻辑回归模型；至少累计 60 个有效盘中样本、10 个交易日且正负样本齐全后才允许比较
- 候选模型必须按时间切分做样本外验证，只有样本外 Brier 分数优于当前模型至少 0.01 才自动升级；否则继续使用当前模型并保留候选评估记录
- 炸板和回封属于高波动高风险事件，不接入实盘交易；概率不是保证，数据过期、样本不足或流动性异常时不得给出高置信度买入建议

### 2.14 短线与波段策略自优化

- 自动回测同时承担短线与波段排名策略的版本化观察和渐进优化，活动版本分别独立管理，初始版本为短线 v1.0 和波段 v1.0
- 每个交易日除保存页面展示的前三外，再分别保存短线与波段前 20 名的真实候选快照用于训练；训练候选不得冒充页面发现记录
- 训练候选同样必须具有真实行情时间或独立交易日历确认的交易日期；缓存只有抓取时间时跳过当日采样，禁止把同一批旧榜单重复算作新交易日
- 候选快照保存股票代码、当日排名、发现价、基础评分、活动版本、完整特征、行情时间和数据源；禁止用未来数据补写发现时特征
- 后续每个有效交易日为尚未成熟的候选保存一次价格观察；短线标签使用后续第 5 个有效观察日，波段标签使用后续第 15 个有效观察日
- 成熟标签同时包括期末涨跌幅、观察期最大回撤和上涨与否，不得只用胜率作为优化目标
- 短线累计 200 个成熟样本、波段累计 300 个成熟样本后立即执行候选优化，不再以累计交易日数量作为训练阻断条件；交易日覆盖数继续展示并作为结果可信度提示，正负样本必须齐全
- 每次训练优先按发现日期保持时间顺序切分，前段训练、后段验证；交易日较少时仍须保留严格靠后的样本作为验证集，不得把验证样本重新用于拟合，也不得因为未达到固定交易日数量而跳过优化
- 候选模型以发现后的实际涨跌结果为监督信号，同时纳入期末收益、观察期最大回撤和上涨方向，综合使用评分、置信度、短线/波段得分、涨跌幅、换手、量比、流动性、估值和风险质量等多因素；AI/机器学习只负责生成可验证的候选权重，是否升级始终由后续真实样本外结果决定
- 候选版本只允许调整排名特征权重与有限评分修正，不得自动改写 Python 代码、风险硬过滤、成交规则或数据源
- 候选版本以收益率和成功率为主要升级目标：验证集前三平均收益至少提高 0.5 个百分点、上涨比例不得下降，平均最大回撤允许最多恶化 10 个百分点；三项结果均通过后才自动启用
- 短线优化额外纳入全市场涨跌家数、市场平均涨跌和板块广度等环境因子；当后段验证前三平均收益仍为负或成功率低于最低可信门槛时，即使相对旧版有所改善也不得自动升级
- 每次成熟样本更新后自动比较短线与波段的统一口径收益、成功率和回撤；短线持续落后时记录原因并优先校准市场环境过滤与排名权重，不得通过改写旧样本掩盖落后
- 未通过门槛的候选版本仍保存评估记录并标注拒绝原因；活动版本始终可以追溯与回滚
- 每条前三发现记录展示当时使用的策略版本；自动回测页面必须同时突出显示“最新优化版本”和“当前生效版本”，候选优化完成后版本号即递增并标注已通过或未通过，不得因候选未上线而让用户误以为没有优化；每个版本号必须可点击，首次点击展示该版本的训练/验证样本量、收益率、成功率、最大回撤、相对改进和启用状态，再点击“查看实际选股结果”展示该版本逐日实际选中的股票、发现日期、训练/验证分组、最终涨跌和最大回撤；同时展示成熟样本量、距离升级门槛、最近候选结果和版本历史
- 短线与波段策略进化卡必须同时展示成熟样本、跟踪中样本和当前最长有效观察进度；波段尚未达到 15 个后续有效交易日时明确显示真实积累进度，不得因成熟样本为零而呈现为空白，也不得提前把未成熟样本计为成熟
- 新版本启用后只影响未来榜单排序，禁止回写旧榜单、旧建议或旧回测结果
- 自优化属于受约束的滚动参数校准，不代表策略会持续变好；市场环境改变、样本不足或公共版历史丢失时必须停止升级并明确提示
- 自动回测主页面按发现日期折叠分组；已购入标的脱离日期分组在顶部单独展示，其余记录继续正常积累
- 策略进化详情必须可展开查看每次优化实际使用的样本日期、股票、发现时特征、后续观察、成熟标签、训练/验证切分、候选参数、验证指标与通过或拒绝原因
- 初始版本或尚未产生候选优化记录时，版本详情仍必须汇总该版本已经保存的真实候选样本与逐日观察，分别显示可用样本、成熟样本和跟踪中样本；成熟样本可展示阶段性表现，但必须明确标注其不是正式样本外验证结果，并允许查看实际股票及后续走势
- 排名候选特征可纳入在发现时已经公开且可追溯的新闻情绪、板块整体强度和个股相对板块同类股票的强弱；新闻缺失时使用中性值并降低可信度，禁止使用发现日之后的新闻或收盘后信息回填历史特征
- 新闻、板块与同行因子只参与受约束的候选权重校准，不能绕过硬风险过滤；是否升级仍由后续真实结果和严格时间切分的样本外验证决定
- 完成一次优化后允许清理不再需要的原始临时数据，但上述优化审计快照必须保留供用户检查，禁止只保留结论

### 2.15 连板与跌停研究

- 新增独立“连板与跌停研究”页面，分为连板池和跌停池两个页签，结构、每日预测快照、收盘复盘、版本管理和受约束自优化与炸板研究一致
- 连板池预测下一交易日继续涨停并晋级连板的概率；跌停池预测下一交易日打开跌停并出现价格修复的概率
- 每个交易日、每个池最多保存和展示排名前 10 的合格候选；低置信度、数据缺失、流动性异常或无法交易时允许少于 10 只
- 候选因子至少覆盖连板高度、封板时间、开板次数、封单强度、换手、成交额、流通市值、行业热度、市场涨跌停家数、近期涨停历史、价格位置和次日竞价/开盘可用信息；任何信息只能在实际可用后参与预测
- 跌停修复需要额外考虑跌停原因、连续跌停数、封单、流动性、市场恐慌程度和是否存在重大风险；存在退市、停牌或重大异常风险时不得给出高置信度参与建议
- 预测与结果按时间隔离，使用 Brier 分数、方向准确率、概率分档命中率和样本量评价；满足最小样本与样本外改进门槛后才允许自动升级版本

### 2.16 交易复盘与 AI 锐评

- 新增独立“交易复盘”页面，页面仅保留连续对话记录和一个文字输入框；用户直接用自然语言描述日期、股票、价格、买入/卖出与原因，不要求单独选择股票或填写结构化字段
- 交易记录保存在当前浏览器本地；公共网站其他访问者不可见，不需要账号
- 后端从每轮对话自动识别 6 位代码或中文股票名称，并携带最近对话上下文继续分析；无法唯一识别股票时必须先追问，不得生成目标价或止损价
- 用户主动点击后才调用后端 DeepSeek；AI 分析买卖点、仓位、止损、机会成本、执行纪律和可改进之处，并明确指出更合理的选择及其可核对依据
- 锐评必须优先输出可执行的当前措施，包括明确操作（加仓、持有、减仓、清仓或等待）、参考目标价、第二目标价、止损价、加仓或重新买入区间、建议仓位、预计观察周期和触发条件；价格依据必须来自可核对行情，数据不足时返回空值并说明，禁止臆造精确价格
- AI 不得编造当时不可用的行情；需要历史数据时按交易时间获取，并将数据时间、引用指标、风险和不确定性与锐评同时展示
- DeepSeek 不可用时保留记录并明确提示，不生成虚假报告

### 2.11 开源项目借鉴与归因

- TradingAgents：借鉴多角色研究、多空辩论、交易员和风险经理的决策流程
- daily_stock_analysis：借鉴 A 股日常分析、策略问股、报告结构和操作检查清单
- QuantDinger：借鉴策略实验、风险参数、回测与运行状态设计，不引入其实盘交易功能
- Microsoft Qlib：借鉴多因子排序、滚动训练和样本外验证方法
- 只引入许可证允许且与本项目范围一致的思想或代码；复制或修改代码时保留许可证、版权和修改说明
- 禁止复制 QuantDinger 的商标、品牌和受单独许可证约束的前端
- 第三方来源、许可证、版本或提交号记录在 THIRD_PARTY_NOTICES.md

---

## 三、预设策略体系

原有十个策略全部废弃，不再作为默认策略或组合成员。新的内置策略为：

| 策略 | 模式 | 核心逻辑 | 主要借鉴 |
|------|------|---------|---------|
| 市场顺风趋势 | 短线/波段 | 大盘环境、趋势强度、均线斜率和波动共同过滤 | Qlib、daily_stock_analysis |
| 波动调整动量 | 短线 | 用收益动量除以 ATR/波动率，排除冲高和过热 | Qlib 多因子思想 |
| 高质量突破 | 短线 | Donchian 突破、ADX、量价确认和 ATR 风控 | QuantDinger 策略实验思想 |
| 趋势内均值回归 | 短线 | 只在上升环境中做布林带/RSI 回归，避免接下跌飞刀 | daily_stock_analysis |
| 资金累积确认 | 短线/波段 | OBV、MFI、量价累积和流动性过滤 | TradingAgents 技术分析角色 |
| 质量成长合理价 | 波段 | ROE、营收利润增长、估值和趋势联合筛选 | TradingAgents 基本面角色 |
| 多因子排名 | 波段 | 动量、质量、价值、低波、流动性横截面排序 | Microsoft Qlib |
| 风险平衡综合 | 短线/波段 | 组合多个独立信号，并按市场环境动态调整权重 | TradingAgents 风险经理 |

所有预设策略必须有市场环境过滤、流动性过滤、风险退出和可解释条件。AI 联合分析不计入确定性策略胜率，单独展示。

---

## 四、评分与买入建议

### 4.1 短线评分

| 维度 | 权重 | 指标 |
|------|------|------|
| 短期趋势 | 25 | 均线、近期高低点、突破状态 |
| 动量强度 | 20 | 涨速、区间涨幅、MACD、RSI、KDJ |
| 量价配合 | 20 | 量比、成交额、换手、放量或缩量 |
| 盘中表现 | 15 | 分时均线、盘中位置和价格强弱 |
| 形态位置 | 10 | 突破、回踩、支撑压力和乖离率 |
| 风险控制 | 10 | 波动、追高、流动性和异常状态 |

### 4.2 波段评分

| 维度 | 权重 | 指标 |
|------|------|------|
| 中期趋势 | 25 | MA20、MA60、趋势斜率、阶段高低点 |
| 盈利质量 | 20 | ROE、利润质量和盈利稳定性 |
| 成长能力 | 15 | 营收和净利润增长 |
| 估值水平 | 15 | PE、PB 及行业相对位置 |
| 量价资金 | 10 | 活跃度和量价结构 |
| 买入时机 | 10 | 回调位置、突破确认和技术动能 |
| 风险控制 | 5 | 回撤、波动、财务和交易异常 |

### 4.3 建议等级

| 建议 | 基础条件 |
|------|---------|
| 建议买入 | 总分不低于 80，核心趋势成立且无硬性风险 |
| 建议小仓位试买 | 70–79 分，信号成立但仍有待确认因素 |
| 建议观察 | 60–69 分，存在机会但条件不完整 |
| 暂不建议 | 低于 60 分，或趋势、量价、风险不支持 |
| 建议回避 | 触发重大风险、数据异常或不可交易 |

评分阈值需要通过历史验证持续校准。每条建议必须同时返回：

- 适用模式与建议等级
- 3–5 条依据、1–3 条风险
- 数据时间、参考关注区间和止损参考
- 信号失效条件
- 评分置信度；数据缺失越多，置信度越低

### 4.4 硬性风险过滤

出现以下情况时默认不得输出建议买入：

- 停牌、退市整理或无法交易
- 关键数据缺失、过期或异常
- 流动性过低
- 连续涨停导致无法合理买入
- 短期涨幅或乖离率过大
- 上市时间不足且历史数据不够
- 重大财务异常

ST、北交所、科创板和次新股可以查询；进入建议范围时必须显示醒目风险提示。

---

## 五、实时数据规则

### 5.1 更新频率

- 交易时间最新行情默认每 10 秒轮询
- 浏览器每次首次打开或整页刷新时自动调用统一数据刷新检查，刷新行情、自动回测、炸板和连板跌停等相关数据；服务端设置冷却和互斥，访问者不能绕过冷却反复触发全市场抓取
- 分钟 K 线按数据源能力更新
- 日线和评分收盘后更新，也允许手动更新
- 财务数据按披露周期更新

### 5.2 数据质量

- 响应包含行情时间、获取时间、数据源和缓存状态
- 失败时可返回最后有效缓存，但必须显示缓存时间
- 不得把系统时间冒充行情时间
- 不得用随机或模拟数据填补真实行情
- 对价格、成交量和涨跌幅做合理性检查

### 5.3 数据源

- 第一版以 AKShare 免费接口为主
- 全市场实时行情优先使用东方财富，失败时自动切换腾讯证券
- 腾讯全市场接口较慢，备用状态至少缓存 60 秒；前端仍按 10 秒检查并显示真实缓存时间
- 已有缓存时先立即响应，过期数据在后台刷新，避免页面长时间卡住
- 日、周、月 K 线在东方财富失败时切换腾讯；分钟 K 线暂时只使用东方财富
- 数据访问放在独立适配层，页面与评分不直接依赖具体接口
- 为未来收费行情或备用数据源预留切换能力
- 同花顺 iFinD 仅作为可选授权数据源：只有配置独立 iFinD access token/refresh token 并通过连通性测试后才能启用；DeepSeek 密钥不能代替行情授权，未配置时继续使用现有免费数据源
- 公共网站阶段由后台统一采集和缓存，访问者不能直接触发全市场抓取


### 5.4 回测成交与审计规则

- 日线策略在当日收盘后完成信号，最早只能在下一可交易日执行；不得使用当日收盘价成交。
- 买入和卖出均计入可配置滑点；佣金双边收取，印花税仅在卖出时收取。
- 止损或止盈发生跳空时按实际开盘价保守成交，不得假设仍能以预设触发价成交。
- 一字涨停、无成交量或价格字段异常时不得假设可以买入；一字跌停时不得假设可以卖出。
- 日线净值按每日收盘价盯市，最大回撤、波动率和夏普比率不得只在平仓日更新。
- 财务、行情和标签均按可用时间并入；任何字段的数据可用时间不得晚于信号或下单时间。
- 策略实验必须记录股票池、因子公式、参数、样本区间、成交规则、费用和风险规则；盲测开始后不得按结果追改。
- 样本外结果重复用于筛选或调参后，不再视为真正盲测；需要另留最终测试集或使用滚动验证。
- 审计至少检查输入顺序、跨股票串行、数据不足、shift 方向、零分母、缺失值、确定性、集中度、行业市值暴露、多重检验和样本外污染。
- 回测收益高不代表实现正确；上线前必须通过可手工核对的成交与净值单元测试。

---

## 六、界面设计

主导航：今日机会、板块热力图、自动回测、炸板研究、连板与跌停、AI 联合分析、交易复盘、策略工坊、策略实验室、已购入、数据与设置。条件选股、独立股票详情、综合评分榜与自选股不再显示。

- 视觉身份以 DESIGN.md 为准：深空蓝黑画布、青色主高光、紫色环境光，形成克制的科幻数据驾驶舱
- 红涨绿跌，符合 A 股习惯；风险、警告和失效条件不得被装饰弱化
- 卡片、表格、图表和表单保持原有信息结构与操作路径，不因视觉升级删减或移动功能
- 页面切换、卡片反馈和环境光采用轻量流畅动画，并完整支持 prefers-reduced-motion
- 数字、代码和指标使用等宽字体与等宽数字；正文优先保证中文可读性
- 买入建议醒目，指标带大白话解释
- 桌面浏览器优先，同时适配手机
- 桌面端首次加载和整页刷新时不得因响应式断点尚未初始化而误折叠侧边栏；菜单、标题和左侧文字必须保持可见，页面不得产生整体水平偏移
- 加载、空数据、过期和错误状态均有明确说明
- 所有 Alert、Modal 等覆盖式提示必须提供清晰可见的关闭按钮
- 非用户主动触发的相同提示在关闭后按本地交易日期记忆，当日切换页面或刷新不得重复出现，下一交易日自动恢复

---

## 七、技术架构

| 层级 | 技术 | 用途 |
|------|------|------|
| 前端 | React + TypeScript + Vite | 本地网站界面 |
| UI | Ant Design | 表单、表格、导航和反馈 |
| 图表 | ECharts + lightweight-charts | 指标、收益和 K 线 |
| 后端 | Python + FastAPI | 行情、筛选、评分和回测 |
| 分析 | pandas + numpy | 指标、因子和数据处理 |
| AI | DeepSeek OpenAI 兼容 API | 多角色联合分析与结构化报告 |
| 外部研究 | TradingAgents、daily_stock_analysis、QuantDinger、Qlib | 仅按许可证借鉴与改写 |
| 数据 | AKShare | 免费 A 股行情与基本面 |
| 数据库 | SQLite + SQLAlchemy | 行情、财务、评分和更新记录 |
| 浏览器存储 | localStorage 或 IndexedDB | 自选股和筛选方案 |

第一版不使用 Tauri，不生成桌面安装包；启动本地服务后直接用浏览器访问。

- Windows 用户双击根目录“启动网站.bat”后，前后端以隐藏后台进程运行并自动打开浏览器；启动窗口关闭后服务继续运行，重复启动应复用已监听的服务，电脑关机后下次开机需重新运行一次。
- 可运行根目录“设置开机自动启动.bat”，为当前 Windows 用户安装登录启动项；登录后隐藏启动本地服务并打开网站，不需要管理员权限，同时提供“取消开机自动启动.bat”撤销。

数据流：

    React 浏览器界面
        ↓ HTTP
    FastAPI 本地服务
        ├─ 实时行情适配器 → AKShare
        ├─ 更新服务 → SQLite
        ├─ 指标、因子与市场环境引擎
        ├─ 选股服务
        ├─ 样本外回测与策略实验室
        └─ DeepSeek 多角色联合分析

未来公共网站：

- GitHub 保存和管理代码，不承担 Python 行情服务
- 前端部署到网页托管平台
- FastAPI 部署到云服务器或容器平台
- SQLite 升级为 PostgreSQL
- 增加缓存、后台任务、限流、日志、监控和密钥管理
- 登录、会员和收费不属于当前版本

### 7.1 当前公开部署

- GitHub 公开仓库保存代码并触发自动部署
- 每次公共发布后必须核验 GitHub 最新提交、Render 实际部署版本以及自动回测、炸板等关键接口；只推送 GitHub 但公共服务仍运行旧版本不得视为发布完成
- Render 免费 Web Service 使用 Docker 同时运行前端静态文件和 FastAPI
- DeepSeek 密钥只保存为 Render Secret，不写入仓库或前端
- 免费实例的 SQLite 属于临时存储，重启后允许重建缓存；升级正式服务时迁移 PostgreSQL
- 每日推荐发现快照额外使用 GitHub 按交易日归档作为免费阶段的持久化恢复层；SQLite 重建后必须先导入随版本发布的归档，并可从公开归档索引补齐本地缺失日期
- 健康检查使用 `/api/health`，服务监听平台提供的 `PORT`
- `/api/health` 必须返回当前运行的 Git 提交号、分支和仓库标识；公共发布只有在该提交号与 GitHub `main` 一致且关键业务接口验证通过后才算完成
- GitHub `main` 推送后使用仓库 Secret `RENDER_DEPLOY_HOOK_URL` 触发 Render 部署并校验实际运行提交；Secret 不得写入代码、日志或前端

---

## 八、核心数据表

- stocks：代码、名称、市场、板块、行业、上市日期、ST 标记和交易状态。
- quotes：价格、涨跌、成交、换手、量比、行情时间、数据源和缓存状态。
- bars：以代码、周期、时间为唯一键，保存开高低收、成交量和成交额。
- fundamentals：PE、PB、ROE、增长率、市值、报告期和更新时间。
- scores：模式、总分、分项分、建议、理由、风险、置信度、数据时间和版本。
- data_update_jobs：任务类型、开始结束时间、进度、成功数、失败数和错误摘要。
- strategies：策略名称、类型、模式、买卖条件、风险参数、组合成员、权重、触发分数和内置标记。
- financial_periods：股票代码、报告期、可用日期、ROE、营收增长、利润增长、每股收益、每股净资产和数据源。
- ai_analysis_runs：股票代码、分析时间、模型、分析深度、输入快照摘要、结构化结论、完整报告、耗时和状态。
- strategy_evaluations：策略、样本区间、样本类型、市场阶段、交易数、胜率、盈亏比、期望值、收益、回撤和夏普比率。
- ranking_discoveries：发现日期、模式、当日排名、股票代码与名称、行业、发现价、发现时评分、建议、置信度、理由、风险、行情时间、数据源和记录时间。
- ranking_advice_snapshots：榜单发现记录、建议日期、当前价、发现后涨跌幅、当前评分、操作建议、目标仓位、置信度、依据、风险、失效条件、行情时间和模型版本。
- limit_break_events：交易日、股票、行业、预测阶段、首次封板与观测时间、价格与涨停价、炸板次数、振幅、换手、成交额、市值、市场封板率、特征快照、预测概率、建议、模型版本、最终是否回封和复盘说明。
- limit_break_model_versions：模型版本、类型、参数、训练截止日、训练与验证样本数、Brier 分数、准确率、启用状态和创建时间。
- ranking_strategy_versions：模式、版本、参数、训练截止日、训练与验证样本数、验证收益、回撤、上涨比例、状态、启用时间和说明。
- ranking_training_samples：交易日、模式、股票、候选排名、发现价、基础评分、策略版本、发现时特征、目标观察日数、成熟标签和数据源。
- ranking_training_observations：训练样本、观察日期、价格、相对发现价涨跌幅和行情时间。
- ranking_optimization_runs：模式、运行日期、现用版本、候选版本、样本数量、验证指标、是否通过、拒绝原因和完成时间。
- ranking_optimization_audits：优化运行、样本日期、训练/验证分组、股票、发现时特征、观察序列、成熟标签、候选得分与审计摘要。
- board_pool_events：交易日、池类型（连板/跌停）、股票、排名、预测时间、当时可用特征、预测概率、建议、模型版本、次日结果和复盘说明。
- board_pool_model_versions：池类型、版本、参数、训练截止日、训练与验证样本数、Brier 分数、方向准确率、启用状态和创建时间。

实际字段增删时必须同步更新本节。

---

## 九、明确不做

- 实盘交易和券商账户连接
- 模拟交易
- 注册、登录、会员和收费
- 云端同步
- 社交、跟单和社区
- AI 预测涨停或保证收益
- 逐笔成交级行情
- 港股、美股、基金和期货
- 用户编写代码的策略编辑器

---

## 十、开发阶段

1. 新项目基础：移除旧代码，搭建 React、FastAPI、新数据库和统一接口。
2. 数据行情：股票搜索、实时行情、分钟及日周月 K 线、缓存和更新状态。
3. 评分建议：指标、双评分、建议解释、风险和预设方案。
4. 主要页面：今日机会、选股、评分榜、详情、自选和设置。
5. 策略验证：历史收益、风险、基准对比和信号明细。
6. 开源研究与策略替换：审查许可证，重写多因子策略并记录归因。
7. AI 联合分析：接入 DeepSeek，多角色报告、历史记录和安全降级。
8. 策略实验室：滚动样本外回测、市场阶段拆分和策略对比。
9. 质量发布：测试、构建、视觉检查、启动脚本和说明。

---

## 十一、验收标准

1. 正常、加载、空数据、过期和失败场景均可用。
2. 评分和买入建议能解释来源。
3. 不用模拟数据冒充真实数据。
4. 后端核心计算有自动测试。
5. 前端可成功构建。
6. 用户能按 README 在本地启动。
7. 需求变化已经先记录到本文档。
8. 策略不能只按胜率筛选，必须同时报告交易数、盈亏比、期望值、收益、最大回撤和夏普。
9. 默认展示样本外或滚动验证结果；样本内结果必须明确标注，不得冒充泛化表现。
10. 至少按上涨、震荡、下跌市场阶段分别验证，不得只挑有利年份。
11. AI 密钥只由后端读取，前端、接口响应、日志、数据库和 Git 均不得出现完整密钥。
12. 所有外部项目借鉴均有许可证与来源记录。
13. 所有直接列举股票的表格或股票篮子都应提供删除按钮；删除后其他股票保持原顺序，历史审计数据不得因界面删除被改写。

---

## 十二、协作规则

### 12.1 需求确认

1. 用户允许助手在任何任务开始前提出问题。
2. 只要关键需求不清楚，助手可以继续提问，直到双方理解一致。
3. 重大技术决策要用大白话说明方案与影响，由用户决定。
4. 已确认需求不得自行缩减，确需调整时先向用户说明。

### 12.2 开发规则

1. 修改前先说明改什么和为什么。
2. 需求、数据库或设计变更先更新本文档。
3. 先完成可运行的完整链路，再优化细节。
4. 每阶段完成后提供测试方法。
5. 代码注释使用中文。
6. 不沿用旧项目业务代码，从零实现。
7. 删除或覆盖重要数据前核对目标。

### 12.3 投资信息规则

1. 网站可以给出建议买入等明确建议。
2. 建议必须来自可查看的数据规则，并展示风险和失效条件。
3. 数据不完整、过期或异常时不得给出高置信度买入建议。
4. 页面固定提示：量化结果仅基于历史和当前公开数据，不保证未来表现，投资决策和风险由用户自行承担。
5. 不以提高胜率为唯一优化目标；低频、过拟合或盈亏比差的高胜率策略不得宣称更优。
6. AI 报告属于非确定性研究意见，必须与可回测量化信号分开展示。

---

## 十三、变更记录

| 日期 | 变更内容 |
|------|---------|
| 2026-07-23 | 创建旧版量化交易系统文档 |
| 2026-07-27 | 放弃旧桌面应用，重新定义为本地量化选股网站 |
| 2026-07-27 | 确认覆盖全部 A 股，聚焦短线与波段，不做登录和模拟交易 |
| 2026-07-27 | 确认交易时间实时行情默认每 10 秒刷新，并提供分钟 K 线 |
| 2026-07-27 | 确认双评分，并允许输出带依据和风险的买入建议 |
| 2026-07-27 | 实时行情确定为东方财富优先、腾讯备用；缓存先响应并在后台刷新 |
| 2026-07-27 | 日周月 K 线增加腾讯备用源，历史缓存按回测范围自动补齐 |
| 2026-07-27 | 个股图表改为分时、日周月 K，并增加可开关均线 |
| 2026-07-27 | 个股页增加策略回测，买卖点直接叠加到日 K |
| 2026-07-27 | 新增无代码策略工坊、条件策略、组合权重和综合策略 |
| 2026-07-27 | 基本面策略回测增加历史时点财务缓存，禁止未来数据 |
| 2026-07-27 | 确认废弃原十个内置策略，改用市场环境、多因子和风险平衡策略 |
| 2026-07-27 | 确认接入 DeepSeek 可选 AI 联合分析，普通量化功能保持独立 |
| 2026-07-27 | 确认研究 TradingAgents、daily_stock_analysis、QuantDinger 和 Qlib，并遵守许可证与归因要求 |
| 2026-07-29 | 按 Remotion 分阶段编排原则升级首次进入、页面切换、数据加载和图表加载动画，不改变业务功能 |
| 2026-07-28 | 确认将完整项目代码上传至 GitHub 公开仓库；密钥、数据库、缓存、虚拟环境和研究源码不得上传 |
| 2026-07-28 | 确认使用 Render 免费 Web Service 公开部署，采用 Docker 单服务提供 React 与 FastAPI |
| 2026-07-28 | 修复 Render Node 环境与 npm 锁文件严格校验不兼容导致的首次构建失败 |
| 2026-07-29 | 增加回测成交与审计规则：信号次日执行、滑点与印花税、涨跌停成交限制、跳空处理、每日盯市和样本外防污染 |
| 2026-07-29 | 修复分时均价单位错误，增加时间排序去重、分时日期与数据源提示及 10 秒静默刷新 |
| 2026-07-29 | 统一所有股票搜索入口，支持 6 位代码、中文名称、完整拼音和拼音首字母 |
| 2026-08-05 | 删除重复的综合评分榜；今日机会、炸板、连板和跌停候选均采用最多 10 只且允许不足额的质量优先规则 |
| 2026-08-05 | 自选股由本地“已购入”替代；自动回测按日期折叠，并保留可检查的策略优化样本与审计明细 |
| 2026-08-05 | 新增连板/跌停研究与交易复盘 AI 锐评；炸板顶部指标支持下钻且统一按每日前 10 有效预测统计 |
| 2026-07-29 | 采用 Data Drift 科幻数据驾驶舱视觉，增加流畅页面与交互动效，所有功能和操作路径保持不变 |
| 2026-07-29 | 新增自动回测：每日留存短线与波段前三发现快照，跟踪发现价至当前价的真实涨跌表现 |
| 2026-07-30 | 自动回测增加逐日加仓、持有、减仓、清仓建议；新增炸板研究、盘中回封概率、收盘复盘和受样本外门槛约束的渐进模型校准 |
| 2026-07-30 | 短线与波段排名增加独立版本、前 20 候选留存、5/15 日成熟标签和受收益回撤联合门槛约束的自动升级 |
| 2026-07-30 | 所有股票列表增加单股删除与原序顺位；修复缓存抓取时间被误当交易日导致机会榜跨日重复，快照和训练样本改为强制校验真实行情时间 |

| 2026-08-01 | 自动回测主列表紧凑化并保留顶部指标、策略版本与进化进度；弹窗和提示统一增加关闭按钮；移除减仓并增加连续清仓三日归档；提高炸板筛选门槛；页面打开自动预热行情缓存 |

### 2.17 真实历史算法研究与标签质量升级（2026-09-07）

- 基于真实A股历史日线构建最近一年独立研究管线；不足一年、退市股票缺失、历史ST状态不全、样本池与当前全市场不同等限制必须逐项披露，不得称为无偏全市场回测。
- 优先落实交易日期和收盘观察核验、峰谷最大回撤、无标签重叠的时间验证；旧发现记录、旧成熟标签和旧版本审计保持不变，新评价采用独立版本，疑似旧标签不能自动用于新版本升级。
- 新观察必须核验收盘行情时间，生成时间和抓取时间不得冒充交易时间；周末/节假日不新增交易日建议或训练结果。日期证据不足时只读跟踪并明确等待。
- 新候选只采用真实有覆盖、非恒定的少量因素；缺失新闻、行业与市场因素不以默认中性值作为有效训练数据。候选包含市场状态过滤、排序以及独立可成交入场/退出实验。
- 一年研究的技术基准必须明确为当前算法可由历史日线重建的部分；无法还原历史财务、盘中量比、新闻和AI时不得冒充完整线上荐股算法复现。原始推荐样本验证和一年技术研究分别报告。
- 开发迭代只在开发区间进行；按时间滚动验证并清除跨越测试预测时点的训练标签。最终保留区间在选择方案前锁定且只评估一次；失败后不得继续针对该保留区间调参并宣称样本外成功。
- 本次研究预先定义较大提升：最终同窗净收益和已平仓交易胜率均提高至少5个百分点、峰谷最大回撤不恶化、至少30笔已平仓交易且覆盖至少20个实际入场日期；同时遵守原有样本量、绝对收益、成功率与硬风险升级门槛。失败时保存拒绝及下一步，不能保证必然找到有效算法。
- 可成交研究采用收盘信号次日执行、A股T+1、100股整数手、固定资金预算、单股单仓、最多持仓数、涨跌停及停牌限制、跳空实际成交、佣金最低额与滑点税费；基准和候选使用相同资金及成本。日线无法确定同日先止损还是先止盈时按不利顺序计算。
- 历史研究与未来影子候选允许执行，只有独立最终验证及数据质量门槛全部通过才可新版本启用；研究排名不得绕过线上风险过滤或覆盖旧榜单。
- 新增研究文件及逐笔交易、净值、数据来源/抓取时间、参数与拆分清单留在本地研究目录，禁止上传密钥、原始数据库或用户交易记录。
- 自动回测的策略进化卡同时展示一年技术研究的保留区间基准/候选收益、胜率与回撤，以及未启用原因；全年包含开发区间的回放不能冒充全部样本外结果。

### 2.18 新旧方案并行研究（2026-09-07 用户授权）

- 并行研究接受原始盘口盘后时间，必须用同日日线收盘交叉核验；原训练标签的默认时间规则保持不变。

- 新增独立菜单“新旧方案对比”，启用已冻结的短线 short-baseline-r3 与波段 swing-baseline-r2 作为研究方案；旧今日机会、活动版本和历史记录继续保留。研究部署不等于通过自动升级，不接入实盘交易。
- 数量以质量为先，没有最低推荐数量；沿用研究每模式最多三只的风险上限，允许一只、两只或零只，不补位、不降阈值。市场、数据或个股风险不通过时显示明确空榜原因。
- 实时研究使用相同日线因子和冻结参数，市场上涨占比、站上20日均线占比均至少45%，技术分至少70。还需经过现有线上个股硬风险与置信度检查；此额外检查明确标记为试运行保护，不冒称已计入原一年回测。
- 每次研究以已核验收盘日期为准，整批日线需覆盖至少90%的当次行情样本池；缺失或非同日数据不进入榜单。旧日期只能显示历史信号，不得冒充今日买入建议。
- 页面展示短线/波段、最后验证期与全年回放的收益曲线、扣费组合收益、胜率、最大回撤、交易数和已保存逐笔记录；技术基准与旧网站完整荐股明确区分，全年含调参区间且不得声称全部样本外。
- 新收盘研究同时保留旧网站当时机会榜、研究候选、排除原因、数据覆盖与时间、冻结参数和来源，按日期不可覆盖保存。后续显示发现收盘至最新已核验收盘的涨跌，仅是信号跟踪，不冒充扣费成交收益。
- 刷新在后台进行，设置互斥与冷却；失败保留历史并显示错误。研究页面定时查看任务进度，不反复发起全市场下载。历史研究展示所需精简公开行情衍生数据随应用打包，原始行情、数据库、密钥及用户记录不发布。
- 公共版将并行研究的公开收盘快照保存至GitHub归档，服务重启后读取随应用打包的归档；浏览器关闭时本地运行中的服务仍定时检查收盘。个股后续涨跌必须使用同一次获取的复权历史与当前收盘作比较，不能混用不同日期抓取的前复权因子。
